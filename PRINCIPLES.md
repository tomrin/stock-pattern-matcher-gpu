# stock-pattern-matcher-gpu 原理与实现说明

## 1. 项目目标

本项目用于查找 A 股历史上的相似走势形态。

输入内容包括：

- 股票代码
- 目标区间起始日期
- 目标区间结束日期
- 返回的相似结果数量

程序会在本项目 `data/kline/` 中的全部历史行情数据里搜索相似窗口，并输出 Top-N 结果以及 HTML/SVG K 线报告。

本项目是历史形态检索工具，不是预测模型，也不构成投资建议。

## 2. 为什么不直接比较价格

不同股票的绝对价格没有可比性。例如：

- 股票 A：10 元涨到 11 元
- 股票 B：100 元涨到 110 元

两者价格不同，但涨幅都是 10%。如果直接比较价格，会把价格尺度误认为走势差异。因此程序比较的是每个交易日的相对变化和结构。

## 3. 目标窗口

用户输入的是自然日范围，例如：

```text
起始日期：20260501
结束日期：20260530
```

程序会在这个自然日范围内只使用实际存在的交易日。周末、节假日不会补造 K 线。

相似历史窗口使用与目标相同的自然日跨度，并要求实际交易日数量与目标窗口一致，避免不同长度的序列直接比较。

## 4. 特征序列

每个交易日会转换成四类特征：

### 4.1 收盘收益率

```text
当日收益率 = 当日收盘价 / 前一交易日收盘价 - 1
```

它描述每天收盘价相对于前一天的变化方向和幅度，是最重要的特征。

### 4.2 日内振幅

```text
日内振幅 = (最高价 - 最低价) / 收盘价
```

它描述当天价格波动的大小，不受股票绝对价格影响。

### 4.3 K 线实体比例

```text
实体比例 = (收盘价 - 开盘价) / 收盘价
```

正值表示收盘高于开盘，负值表示收盘低于开盘；绝对值可以反映实体大小。

### 4.4 相对成交量

```text
相对成交量 = 当日成交量 / 当前窗口平均成交量
```

它描述当天是放量还是缩量，而不是简单比较成交量的绝对值。

## 5. 标准化处理

四类特征的量纲不同，不能直接相加比较。程序对每个特征序列做窗口内 Z-score 标准化：

```text
z = (当前值 - 序列平均值) / 序列标准差
```

标准化后比较的是“当前日相对于该窗口自身平均水平的偏离程度”。如果某个窗口波动很大，另一个窗口波动较小，也仍然可以比较它们的相对节奏。

如果某个特征在整个窗口内没有变化，标准差接近零，程序将该特征序列视为全零，避免除零错误。

## 6. 相似度计算

目标窗口和候选窗口会形成等长特征向量。程序使用 GPU 计算两者之间的欧氏距离：

```text
距离 = sqrt(mean((候选特征向量 - 目标特征向量)^2))
```

距离越小，表示两段走势在以下方面越接近：

- 每日涨跌节奏
- 波动幅度变化
- K 线实体方向和大小
- 放量、缩量节奏

为了方便阅读，程序将距离换算为展示用相似度：

```text
相似度 = 1 / (1 + 距离)
```

该相似度不是概率，也不是未来走势相同的概率，更不是预测准确率。

## 7. GPU 的作用

程序采用 CPU + GPU 的混合方式：

```text
CPU：读取 JSON 文件
  -> CPU：解析日期、截取自然日窗口
  -> CPU：生成相对特征序列
  -> GPU：批量计算候选窗口距离
  -> CPU：保留 Top-N 结果
  -> CPU：生成报告
```

GPU 主要负责大量向量之间的批量距离计算。文件读取、JSON 解析、日期处理和报告生成仍由 CPU 完成，因此 GPU 不会加速全部步骤。

当前实现使用 PyTorch CUDA，并通过批处理控制显存使用：

```text
--batch-size 2048
```

批大小越大，GPU 计算批次越少，但显存占用越高；Quadro T1000 只有 4GB 显存，显存不足时应降低批大小，例如 `--batch-size 512`。

## 8. 数据与项目独立性

GPU 版本是独立项目：

```text
F:\project\stock-pattern-matcher-gpu
```

代码、数据和报告位置如下：

```text
代码：    F:\project\stock-pattern-matcher-gpu\*.py
数据：    F:\project\stock-pattern-matcher-gpu\data\kline\
报告：    F:\project\stock-pattern-matcher-gpu\reports\
```

项目不运行时访问原 `stock-pattern-matcher` 目录，也不会自动重新下载数据。当前数据是已经复制到本项目中的本地行情数据。

## 9. 排除规则

比较相似性时，任何股票（包括目标股票自身）的候选窗口，只要与目标时间段（起始日至结束日）存在日期重叠，就会被跳过。

目标股票早于或晚于目标时间段的窗口照常参与匹配，因此结果中可能出现目标股票在其他时间段的相似形态。

也就是说，被排除的只是"目标时间段本身"，匹配范围是所有股票在该时间段之外的全部数据。

## 10. K 线报告

程序生成：

- 目标窗口 K 线图
- Top-N 相似历史窗口 K 线图
- 每个相似窗口后续 15 个交易日的 K 线图（可用 `--next-days` 调整），展示该历史形态之后的实际走势，作为判断目标股票后续趋势的参考
- 后续表现汇总：控制台和报告顶部会统计 Top-N 匹配在后续 N 个交易日的表现，包括胜率、累计收益的平均值/中位数/极值、平均最大冲高与平均最大回撤；每个匹配项也会单独标注累计收益
- HTML 汇总页面

每张匹配区间图从对应窗口的起始日期向后展示默认 30 个自然日（可用 `--chart-days` 调整）。窗口结束日之后的区域用于观察历史相似形态后续的大致走势，不参与相似度计算。后续交易日图只包含窗口结束日之后的实际交易日，同样不参与相似度计算。

报告使用 SVG 生成，不依赖 matplotlib。每次运行的报告放在独立子目录中，可以直接用浏览器打开：

```text
F:\project\stock-pattern-matcher-gpu\reports\600879_20260506_20260529\report.html
```

## 11. 运行示例

在 `ai_env` 环境中运行：

```powershell
conda activate ai_env
cd F:\project\stock-pattern-matcher-gpu
python .\main.py 600879 --start-date 20260501 --end-date 20260530 --top 10 --batch-size 2048
```

常用参数：

| 参数 | 含义 | 默认值 |
|---|---|---|
| `code` | 股票代码 | 必填 |
| `--start-date` | 目标起始日期 | 必填 |
| `--end-date` | 目标结束日期 | 必填 |
| `--top` | 返回前 N 个结果 | 10 |
| `--batch-size` | GPU 批处理大小 | 4096 |
| `--chart-days` | K 线图展示的自然日数 | 30 |
| `--next-days` | 匹配窗口后续展示的交易日数 | 15 |
| `--data-dir` | 行情数据目录 | 项目自身 `data/kline` |
| `--report` | 报告目录 | 项目自身 `reports` |

## 12. 结果解释与限制

相似历史形态只能说明“过去的变化结构相近”，不能推出未来一定会重复过去。

主要限制包括：

- 数据质量会影响匹配结果
- 复权方式不同会影响收益率和 K 线结构
- 特征选择和权重会影响排序
- 自然日窗口中的交易日数量可能因节假日不同而变化
- GPU 只加速向量计算，整体速度仍受文件读取和 Python 特征构造影响
- 相似度数值只能用于排序，不能当作概率或投资建议

## 13. GitHub 发布建议

建议提交源代码、README、原理说明和数据目录说明，不提交完整行情数据与生成报告：

```text
提交：gpu_matcher.py、main.py、report.py、README.md、PRINCIPLES.md
不提交：data/kline/、reports/、__pycache__/
```

具体忽略规则见项目根目录的 `.gitignore`。公开发布行情数据前，应确认数据源服务条款和数据分发权限。
